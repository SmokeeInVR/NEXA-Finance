## Packages
date-fns | Formatting dates for display
recharts | Visualizing debt payoff projections if needed, though simple math display is requested
lucide-react | Icons for the bottom navigation bar
clsx | Utility for conditional class names (usually installed but good to ensure)
tailwind-merge | Utility for merging tailwind classes (usually installed)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
